import {
  groupArrayItemsInArrays,
  setFieldClassName,
  showFieldErrors,
  setErrorClassName,
  removeItemsFromIndexes,
} from './general';

import {
  capitalizeFirstLetter,
  capitalizeText,
  limitWords,
} from './formatters';

export {
  // General
  groupArrayItemsInArrays,
  setFieldClassName,
  showFieldErrors,
  setErrorClassName,
  removeItemsFromIndexes,

  // Formatters
  capitalizeFirstLetter,
  capitalizeText,
  limitWords,
};
